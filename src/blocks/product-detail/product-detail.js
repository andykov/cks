/* global document */

const ready = require('../../js/utils/documentReady.js');

ready(function(){
  
});

const $ = require('jquery');
$( document ).ready(function() {
    // $('.js-slider').slick();
    // $('.js-gallery').slick();
});
