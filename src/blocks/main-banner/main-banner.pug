//- Все примеси в этом файле должны начинаться c имени блока (main-banner)

mixin main-banner(text, mods)

  //- Принимает:
  //-   text    {string} - текст
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +main-banner('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' main-banner--' + modsList[i].trim();
      }
    }

  .js-main-banner.main-banner(class=allMods data-slick='{"slidesToShow": 1, "slidesToScroll": 1, "dots": true}')&attributes(attributes)
    .main-banner__item(style="background-image: url(img/main-slider-img-1.jpg);")
      .container
        .main-banner__cont
          .main-banner__title Современные интерьерные решения
          .main-banner__text Среди многообразия строительных материалов, применяемых в отделке зданий, особое место занимает натуральный камень
    .main-banner__item(style="background-image: url(img/main-slider-img-2.jpg);")
      .container
        .main-banner__cont
          .main-banner__title Современные интерьерные решения
          .main-banner__text Среди многообразия строительных материалов, применяемых в отделке зданий, особое место занимает натуральный камень  
    block  
